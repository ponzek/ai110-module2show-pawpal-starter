classDiagram
    %% PawPal+ Final UML — matches pawpal_system.py implementation

    class Owner {
        +str name
        +int available_minutes
        +list~str~ preferences
        +list~Pet~ pets
        +add_pet(pet: Pet) None
        +set_availability(minutes: int) None
        +get_pets() list~Pet~
        +get_all_tasks() list~Task~
    }

    class Pet {
        +str name
        +str species
        +int age
        +list~str~ special_needs
        +list~Task~ tasks
        +add_task(task: Task) None
        +remove_task(title: str) None
        +get_tasks() list~Task~
    }

    class Task {
        +str title
        +int duration_minutes
        +str priority
        +str category
        +bool is_recurring
        +str frequency
        +str preferred_time
        +bool completed
        +date due_date
        +is_valid() bool
        +mark_complete() None
        +__lt__(other: Task) bool
    }

    class Scheduler {
        +Owner owner
        +int total_minutes
        +generate_schedule() list~dict~
        +sort_by_priority(tasks: list~Task~) list~Task~
        +sort_by_time(tasks: list~Task~) list~Task~
        +filter_by_pet(pet_name: str) list~Task~
        +filter_incomplete(tasks: list~Task~) list~Task~
        +filter_by_time(tasks: list~Task~, available: int) list~Task~
        +handle_recurring(task: Task, pet: Pet) Task
        +detect_conflicts(schedule: list~dict~) list~str~
        +explain_plan(schedule: list~dict~) str
    }

    Owner "1" --> "1..*" Pet : owns
    Pet "1" --> "0..*" Task : has
    Scheduler "1" --> "1" Owner : plans for
    Scheduler "1" --> "1..*" Pet : schedules tasks for
    Scheduler ..> Task : processes
